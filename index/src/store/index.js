import Vue from 'vue'
import Vuex from 'vuex'
//如果想要发送请求 那么一定要在页面顶部引入axios
import axios from 'axios'
//如果想要使用qs解析对象 那么在顶部引入qs
import qs from 'qs';
import router from '../router'
Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
